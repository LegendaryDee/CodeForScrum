package backEnd;

import java.util.List;
import java.util.ArrayList;

public class DataLoader {
    public static List<User> getUsers() {
        // Your code to load users from a data source
        List<User> users = new ArrayList<>();
        // Parse JSON or DB and populate users list
        return users;
    }

    public static List<Course> getCourses() {
        // Your code to load courses from a data source
        List<Course> courses = new ArrayList<>();
        // Parse JSON or DB and populate courses list
        return courses;
    }
}
