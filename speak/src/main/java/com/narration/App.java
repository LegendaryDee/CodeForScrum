package com.narration;

//
//import org.json.JSONObject;
//import java.nio.file.Files;
//import java.nio.file.Paths;
//import com.narration.Narrator;


public class App {

	/*
	 * public static void main(String[] args) { try { // Adjusted path to your JSON
	 * file in the json folder String jsonFilePath = "json/courses.json"; String
	 * jsonContent = new String(Files.readAllBytes(Paths.get(jsonFilePath)));
	 * 
	 * // Parse JSON content JSONObject json = new JSONObject(jsonContent);
	 * 
	 * // Read and narrate prompts JSONArray prompts = json.getJSONArray("prompts");
	 * for (int i = 0; i < prompts.length(); i++) { String prompt =
	 * prompts.getString(i); System.out.println("Narrating prompt: " + prompt);
	 * Narrator.playSound(prompt); // Use your existing Narrator class }
	 * 
	 * // Read and narrate lessons JSONArray lessons = json.getJSONArray("lessons");
	 * for (int i = 0; i < lessons.length(); i++) { String lesson =
	 * lessons.getString(i); System.out.println("Narrating lesson: " + lesson);
	 * Narrator.playSound(lesson); // Use your existing Narrator class }
	 * 
	 * } catch (Exception e) { e.printStackTrace(); } }
	 */
}
